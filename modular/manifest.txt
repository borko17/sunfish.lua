-- manifest.txt =======

SCRIPT_VERSION = "2.609100232"

CHANGELOG = {
  "Redesigned score display: descriptive text (Even position...Completely winning) plus a tick indicator on the board border, instead of a raw number.",
"Restored the peek-score after the player's move (quiet, small node budget); checkmate now shows max advantage instead of a tiny pre-mate value.",
"Added 'td' command to toggle depth-search print output.",
"Undo ('z') and leaving Challenge mode now correctly restore the displayed score.",
"Fixed bug: pos.score was being needlessly zeroed after Sunfish's move, causing the border indicator to drop to minimum after promoting a pawn to queen.",
}

-- manifest.txt ======= end